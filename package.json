{
	"name": "@levr/v3-site-template",
	"version": "0.1.0",
	"private": true,
	"description": "Template repo cloned by the V3 Agentic Builder when starting a new site. Ships a ~150-line SSI runtime Worker + static partials. Site source-of-truth lives in this repo's levr.json + links.json + public/**.",
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler deploy --dry-run --outdir=dist",
		"deploy": "wrangler deploy",
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"validate-links": "node scripts/validate-links.mjs",
		"render-static": "tsx scripts/render-static.mts"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260801.1",
		"@types/node": "^24.10.1",
		"tsx": "^4.19.0",
		"typescript": "^5.5.0",
		"vitest": "^1.6.0",
		"wrangler": "^4.59.3"
	}
}
